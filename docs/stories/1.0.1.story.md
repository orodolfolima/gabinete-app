---
epic: EPIC-1-SIGGAP-MVP
story_id: 1.0.1
title: Setup Inicial do Projeto
status: Draft
priority: P0
sprint: 0
story_points: 8
executor: "@devops"
quality_gate: "@architect"
quality_gate_tools: [infra_review, docker_validation]

acceptance_criteria:
  - npm install completa sem erros
  - docker-compose up inicia containers locais
  - Frontend rodando em http://localhost:3000
  - Backend rodando em http://localhost:5000
  - .env.example preenchido com todas as variáveis
  - ESLint + Prettier + Husky funcionando
  - README.md com instruções de setup

testing:
  - [ ] Teste: npm install executa sem errors
  - [ ] Teste: docker-compose up sobe todos os containers
  - [ ] Teste: Frontend carrega em http://localhost:3000
  - [ ] Teste: Backend responde em http://localhost:5000/health
  - [ ] Teste: Pre-commit hook bloqueia commits sem lint

dependencies:
  - Docker instalado
  - Docker Compose instalado
  - Node.js 18+

---

# Story 1.0.1: Setup Inicial do Projeto

## 📋 Contexto

Este é o **primeiro story do MVP SIGGAP**. Estabelece a estrutura base do projeto com boilerplate pronto para desenvolvimento.

## 🎯 Objetivo da Story

Configurar ambiente de desenvolvimento local com:
- Repositório Git
- Frontend (React + TypeScript + Tailwind)
- Backend (Express.js + Prisma)
- Docker para ambos
- Ferramentas de qualidade (ESLint, Prettier, Husky)

## ✅ Acceptance Criteria (Detalhado)

### Frontend
- [ ] Projeto React 18 com Create React App ou Vite
- [ ] TypeScript configurado (strict mode)
- [ ] Tailwind CSS instalado e funcionando
- [ ] Shadcn/UI components instalados
- [ ] Rota básica "/" que exibe "Welcome to SIGGAP"

### Backend
- [ ] Express.js server rodando na porta 5000
- [ ] Prisma ORM configurado
- [ ] Rota `/health` retorna `{ status: 'ok' }`
- [ ] .env.example com variáveis necessárias
- [ ] PostgreSQL connection string no .env

### Docker
- [ ] Dockerfile para frontend (multistage build)
- [ ] Dockerfile para backend
- [ ] docker-compose.yml com:
  - Frontend service (port 3000)
  - Backend service (port 5000)
  - PostgreSQL service (port 5432)
- [ ] Health checks configurados
- [ ] Volumes para desenvolvimento local

### Code Quality
- [ ] ESLint configurado com Airbnb config
- [ ] Prettier configurado
- [ ] Husky + lint-staged para pre-commit hooks
- [ ] `.eslintignore` e `.prettierignore` properly set
- [ ] npm run lint executa sem erros
- [ ] npm run typecheck executa sem erros

### Documentation
- [ ] README.md com:
  - Pré-requisitos (Node.js, Docker)
  - Instruções de instalação
  - Como rodar localmente (npm + Docker)
  - Estrutura de pastas explicada
  - Links para documentação
- [ ] .env.example completo com comentários

## 📂 Estrutura de Pastas

```
projeto-root/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── App.tsx
│   │   └── index.tsx
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   └── Dockerfile
├── backend/
│   ├── src/
│   │   ├── routes/
│   │   ├── services/
│   │   ├── utils/
│   │   └── server.ts
│   ├── prisma/
│   │   ├── schema.prisma
│   │   └── migrations/
│   ├── package.json
│   ├── tsconfig.json
│   └── Dockerfile
├── docker-compose.yml
├── .env.example
├── .gitignore
└── README.md
```

## 🔧 Variáveis .env Necessárias

```
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/siggap

# Server
NODE_ENV=development
PORT=5000
FRONTEND_URL=http://localhost:3000

# APIs (deixar vazio por enquanto)
WHATSAPP_API_KEY=
WHATSAPP_PHONE_NUMBER=
N8N_WEBHOOK_URL=
TSE_API_KEY=
GOOGLE_CALENDAR_API_KEY=
```

## 🧪 Como Testar

```bash
# 1. Clone e navegue
git clone <url>
cd projeto-root

# 2. Instale dependências
npm install

# 3. Suba containers
docker-compose up

# 4. Aguarde initialization (30s)

# 5. Teste endpoints
curl http://localhost:5000/health
# Esperado: { "status": "ok" }

# 6. Acesse frontend
open http://localhost:3000
# Esperado: "Welcome to SIGGAP" page

# 7. Teste lint
npm run lint
# Esperado: 0 errors
```

## 🚀 Comandos Principais

```json
{
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "dev": "docker-compose up",
    "dev:down": "docker-compose down",
    "dev:logs": "docker-compose logs -f"
  }
}
```

## ⚠️ Notas Importantes

- **Node.js 18+**: Requerido para ambos frontend e backend
- **Docker**: Essencial para desenvolvimento consistente
- **Husky**: Instalar com `npx husky install` após clone
- **Postgres**: Será iniciado via Docker, sem instalação local necessária

## 🔐 Segurança & Compliance

- [ ] .env nunca commitado (está em .gitignore)
- [ ] .env.example tem apenas placeholders
- [ ] Docker images são oficiais (node, postgres)
- [ ] Não há hardcoded secrets em código

## 📝 Completion Notes

- Marque esta story como ✅ CONCLUÍDA quando:
  - Docker-compose up inicia sem erros
  - Frontend carrega em http://localhost:3000
  - Backend responde em /health
  - npm run lint passa sem erros
  - npm run typecheck passa sem erros

## 🔄 Dev Agent Record

### Setup Checklist
- [ ] Frontend boilerplate criado
- [ ] Backend boilerplate criado
- [ ] Docker files configurados
- [ ] docker-compose.yml funcional
- [ ] ESLint + Prettier instalados
- [ ] Husky hooks funcionando
- [ ] README.md escrito
- [ ] .env.example completo

### Debug Log
(Preenchido durante implementação)

### Change Log
- Versão 1.0: Story criada

---

**Story pronta para implementação por @devops**
